# Supabase Configuration
# Get these values from your Supabase project settings: https://app.supabase.com
VITE_SUPABASE_URL=your-project-url.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Storage Backend Configuration
# Options: 'localStorage' (default) | 'supabase'
# If not specified, defaults to 'localStorage'
# Note: If 'supabase' is selected but credentials are not configured,
# the app will automatically fall back to 'localStorage'
VITE_STORAGE_BACKEND=localStorage
VITE_APP_URL=http://localhost:5173